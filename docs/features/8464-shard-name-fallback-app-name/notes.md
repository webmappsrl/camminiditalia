> Ticket: oc:8464

# Notes — Analytics shard name per query PostHog (repo principale)

## Deviazioni dal piano

Nessuna. Task 3 (`.env-example`) eseguito come pianificato. Task 4 (bump gitlink) resta sospeso, come previsto dal piano, in attesa del push del fix su `wm-package`.

## Bug trovati

Nessuno.

## Decisioni

Vedi `wm-package/docs/features/8464-shard-name-fallback-app-name/notes.md` per il dettaglio tecnico (alternativa "tutto in config" testata e scartata dal vivo durante l'esecuzione).

## Follow-up

- Task 4 (bump gitlink `wm-package`) da eseguire subito dopo il push del fix su `wm-package`.
